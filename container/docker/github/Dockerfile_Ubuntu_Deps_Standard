FROM ubuntu:20.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update \
 && apt-get -y install \
	build-essential \
	curl \
	unzip \
	git \
	python3

WORKDIR /julea-deps

COPY . . 

SHELL ["/bin/bash", "-c"]

RUN ./scripts/install-dependencies.sh
RUN rm -rf /julea-deps
